@model OTTMangementSystem.Entities.Platform

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">
        <strong>Error !</strong>
        @TempData["ErrorMessage"] <button type="button" class="close" data-dismiss="alert" 
                                          aria-label="close" >
            <span>X</span>
        </button>
    </div>
}

<div>
    <h4>Platform</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.PlatformName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.PlatformName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.MonthlyCost)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.MonthlyCost)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.PlatformID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
